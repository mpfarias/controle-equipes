# Copiar para .env.local. IPv4 do PC na mesma Wi-Fi que o telemóvel.
COPOM_LAN_REWRITE=
# O arranque define sozinho, com base no IP: http://SEU-IP:8095 (fixa o manifesto no @expo/cli)
# Só preencha manual se precisar forçar outro endereço (comentar/limpar no tunnel).
# EXPO_PACKAGER_PROXY_URL=
